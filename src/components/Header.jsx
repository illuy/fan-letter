import React from "react";

const Header = () => {
    return (
        <header>
            <div>Login</div>
            <h1>
                <a href="./">LOGO</a>
            </h1>
            <div>Menu Icon</div>
        </header>
    );
};

export default Header;
